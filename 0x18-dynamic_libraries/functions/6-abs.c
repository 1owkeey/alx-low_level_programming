#include "main.h"
/**
 * _abs - as in computes the absolute value of an integer
 * @n: as in the int to check
 * Return: as in the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
